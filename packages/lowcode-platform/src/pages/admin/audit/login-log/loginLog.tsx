import React, { FC, memo } from 'react';

export interface ILoginLogProps {}

export const LoginLog: FC<ILoginLogProps> = memo((props) => {
  return <div>LoginLog</div>;
});

export default LoginLog;

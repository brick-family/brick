import React, { FC } from 'react';

export interface IAggregationProps {}

export const Aggregation: FC<IAggregationProps> = (props) => {
  return <div>Aggregation</div>;
};

export default Aggregation;

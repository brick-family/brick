import React from 'react';
import s from '../iconSelect.less';
import { ProCard } from '@ant-design/pro-components';

export const CustomerPanel = () => {
  return (
    <ProCard className={s.system} bodyStyle={{}} headStyle={{ padding: 0 }} title={''}></ProCard>
  );
};

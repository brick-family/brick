import React, { FC } from 'react';
import { TOperatorCode } from '@brick/types';

export interface IResourceTableWrapperProps {
  OperatorCode: TOperatorCode;
}

export const ResourceTableWrapper: FC<IResourceTableWrapperProps> = (props) => {
  return <div></div>;
};
